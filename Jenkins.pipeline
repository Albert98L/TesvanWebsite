

node {
    stage('npm ci') {
      bat  "npm install"
    }

            stage('Run tests') {
                 bat  "npx cypress run --spec 'cypress/e2e/TesvanHomePage/VerifyHomePage.cy.js'"
           }
      }
