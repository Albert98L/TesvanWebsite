

node {
    checkout scm

    stage('npm ci') {
      sh "npm ci"
    }

            stage('Run tests') {
                 sh "npx cypress run --spec 'cypress/e2e/TesvanHomePage/VerifyHomePage.cy.js'"
           }
      }